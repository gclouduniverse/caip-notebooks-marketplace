# Development

## Build Latest Version

```bash
npm run build
```

## Local Serving

To start server locally just run:

```bash
firebase serve
```

You also might need to clean cache first:

```bash
rm -rf ./.firebase
```
